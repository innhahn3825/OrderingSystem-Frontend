import { TitleBar } from '../../renderer/src/components/Titlebar/TitleBar';

export default {
  title: 'Title Bar',
  component: TitleBar,
};

export const Main = {
  args: {
    Name: 'Food',
  }
};
